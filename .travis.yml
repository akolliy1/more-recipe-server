language: node_js
install:
  - npm install -g sequelize-cli
  - npm install
node_js:
  - "stable"
cache:
 directories:
  - node_modules
before_script:
  - sequelize db:migrate 
after_success:
  - npm run test:coverage
env:
  - NODE_ENV=test